{
  "$comment": "Declarative registry of the Cloudflare API token this repo's deploy broker relies on. METADATA ONLY — the token's secret value lives only in Cloudflare as a Worker Secret, never in GitHub and never in git.",
  "tokens": [
    {
      "name": "cf-broker-bounded-systems-deploy-master",
      "access": "read-write (token-minting root credential)",
      "purpose": "Minting credential for the cf-broker-bounded-systems-deploy Worker — mints short-lived, zone-pinned DNS tokens per GitHub OIDC request from this repo's apply workflow. Not used by any workflow directly.",
      "permissions": [
        "User · API Tokens:Edit",
        "Zone · DNS:Edit (bounded.tools)"
      ],
      "account_id": "a2ca40f474fa65ed8f751a3efdf74c01",
      "zone_resources": "bounded.tools (70b3f6160be73a66b0d4b48aae3f2268) — pinned, single zone",
      "consumer": "cf-broker-bounded-systems-deploy Worker (cf-oidc-token-broker), as env.CF_BROKER_TOKEN",
      "secret_ref": "Cloudflare Worker Secret CF_BROKER_TOKEN on the cf-broker-bounded-systems-deploy Worker",
      "value_location": "Cloudflare only (Worker Secret) — never in GitHub, never in git",
      "created": "2026-06-28",
      "expires": "2027-06-28",
      "rotation": "Auto-expires 2027-06-28. To rotate: mint a replacement with the same scope, `wrangler secret put CF_BROKER_TOKEN --name cf-broker-bounded-systems-deploy`, then revoke the old one. Account-admin-equivalent for minting (API Tokens:Edit isn't subset-limited); direct DNS power is bounded to bounded.tools. Treat any leak as incident-grade.",
      "tracked_by": "prx-k8yc"
    }
  ]
}
